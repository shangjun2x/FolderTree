<Window x:Class="FolderTree.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FolderTree"
        Title="FolderTree" Height="700" Width="1100"
        Background="{DynamicResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" MinWidth="200"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="*" MinWidth="300"/>
        </Grid.ColumnDefinitions>
        
        <!-- Left Panel: Tree View -->
        <Border Grid.Column="0" Background="{DynamicResource SidebarBrush}" 
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <Border Grid.Row="0" Padding="12,10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                            <GradientStop Color="#26458FFC" Offset="0"/>
                            <GradientStop Color="#268C6BFB" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="📁" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="FolderTree" FontSize="15" FontWeight="SemiBold" 
                                   Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                        <Button Grid.Column="2" Content="📂 Open" Click="OpenFolder_Click" 
                                FontSize="12" Padding="10,5"/>
                    </Grid>
                </Border>
                
                <!-- Tree View -->
                <TreeView x:Name="FolderTreeView" Grid.Row="1" 
                          Background="Transparent" BorderThickness="0"
                          SelectedItemChanged="FolderTreeView_SelectedItemChanged"
                          AllowDrop="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            <Setter Property="AllowDrop" Value="True"/>
                            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="TreeItem_PreviewMouseLeftButtonDown"/>
                            <EventSetter Event="PreviewMouseMove" Handler="TreeItem_PreviewMouseMove"/>
                            <EventSetter Event="Drop" Handler="TreeItem_Drop"/>
                            <EventSetter Event="DragOver" Handler="TreeItem_DragOver"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Margin="2">
                                <TextBlock Text="{Binding Icon}" FontSize="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Grid>
        </Border>
        
        <!-- Splitter -->
        <GridSplitter Grid.Column="1" Width="3" Background="{DynamicResource BorderBrush}" 
                      HorizontalAlignment="Stretch" Cursor="SizeWE"/>
        
        <!-- Right Panel: Preview -->
        <Border Grid.Column="2" Background="{DynamicResource BackgroundBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- File Header -->
                <Border Grid.Row="0" Padding="16,12" Background="{DynamicResource SidebarBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock x:Name="FileIcon" Grid.Column="0" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="FileName" FontSize="14" FontWeight="SemiBold" 
                                       Foreground="{DynamicResource TextBrush}"/>
                            <TextBlock x:Name="FileSize" FontSize="11" 
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <RadioButton x:Name="PreviewTab" Content="Preview" IsChecked="True" 
                                         GroupName="Tabs" Margin="0,0,8,0" Click="Tab_Click"
                                         Foreground="{DynamicResource TextBrush}"/>
                            <RadioButton x:Name="SourceTab" Content="Source" 
                                         GroupName="Tabs" Margin="0,0,8,0" Click="Tab_Click"
                                         Foreground="{DynamicResource TextBrush}"/>
                            <Button x:Name="OpenInExplorerBtn" Content="📁" Click="OpenInExplorer_Click"
                                    ToolTip="Show in Explorer" Padding="6,4" FontSize="14"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                <!-- Preview Content -->
                <ScrollViewer Grid.Row="2" x:Name="PreviewScroller" 
                              HorizontalScrollBarVisibility="Auto" 
                              VerticalScrollBarVisibility="Auto"
                              Background="{DynamicResource BackgroundBrush}"
                              Visibility="Collapsed">
                    <Grid x:Name="PreviewContent" Margin="16"/>
                </ScrollViewer>
                
                <!-- Image Preview -->
                <Image Grid.Row="2" x:Name="ImagePreview" Stretch="Uniform" Margin="16" Visibility="Collapsed"/>
                
                <!-- Web Preview (for HTML/PDF) -->
                <WebBrowser Grid.Row="2" x:Name="WebPreview" Visibility="Collapsed"/>
                
                <!-- Placeholder -->
                <StackPanel Grid.Row="2" x:Name="Placeholder" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="📄" FontSize="64" HorizontalAlignment="Center" Opacity="0.3"/>
                    <TextBlock Text="Select a file to preview" FontSize="14" 
                               Foreground="{DynamicResource TextSecondaryBrush}" 
                               HorizontalAlignment="Center" Margin="0,12,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
